# 相机内外参调研结果

## 判定口径

这里的“完整相机内外参”指：公开数据中可以直接获得相机内参 `K`、畸变参数或明确的校正模型，以及相机间外参 `R/t` 或等价的 4×4 变换 `T`。

仅有分辨率、视场角、平面 Homography 或 DLT 投影系数，不按标准完整 `K/D/R/t` 计入。

## 明确提供完整标定的数据集

| 数据集 | 提供范围 | 标定内容 |
|---|---|---|
| **MS² / Multi-Spectral Stereo Dataset** | **全部 sequence** | 每个 `sync_data/<sequence>/calib.npy`。包含 RGB、NIR、Thermal 等相机的内外参，以及相机与 LiDAR/IMU 之间的外参。标定对应已校正（rectified）的发布图像。 |
| **ViViD++** | **`Driving`、`Handheld`** | 官方 Kalibr 标定结果：`driving_results/camchain-rgbther.yaml`、`handheld_results/camchain-therrgb.yaml`。包含相机内参、畸变参数和 RGB↔Thermal 外参。 |
| **TartanRGBT** | **全部序列** | 根目录统一的 `calibration/`：`thermal_left.yaml`、`thermal_right.yaml`、`released_zed_rectified_960x540.yaml`、`cross_camera_extrinsics.yaml`、`tf_static.json` 等。 |

## 只有部分标定，不能按完整标准内外参使用

### LYNRED Mobility

- `Stereovision`：公开页面明确提供 camera registration / extrinsic calibration。
- 但目前公开说明没有明确给出完整数值内参 `K` 和畸变参数，因此不列入“完整内外参”。
- `Multimodal Detection`、`Range Estimation`：不算完整标定。

### BU-TIV

- `Davis 2008`：提供 `calibration_dlt.xml`，包含每个视角的 11 个 DLT 系数，可用于多视图投影/重建，但不是分离的 `K/R/t`。
- `Atrium`：提供 `atriumHomographyExample.m`，只有地面平面 Homography。
- `Lab1`：提供 `labHomographyExample.m`，主要是地面平面 Homography，并附带近似投影矩阵。
- 因此 BU-TIV 没有可直接按标准 `K/D/R/t` 使用的完整相机内外参。

## 未发现公开完整标准内外参

- **FLIR ADAS**：公开发布内容主要是图像、标注、相机型号和视场角，没有官方完整 `K/D/R/t` 文件。
- **LTIR v1**：多来源、多传感器的单目热红外跟踪序列，没有统一相机标定文件。
- **ZUT-FIR-ADAS**：提供热成像帧、标注和车辆/CAN 信息，没有公开完整 `K/D/R/t`。

## 选型结论

如果后续任务需要 RGB↔Thermal 几何投影、深度投影或三维重建，优先使用：

1. **MS²：全部 sequence**
2. **ViViD++：`Driving`、`Handheld`**
3. **TartanRGBT：全部序列**

如果可以接受非标准几何标定：

- BU-TIV `Davis 2008` 可使用 DLT；
- BU-TIV `Atrium` 和 `Lab1` 只能使用 Homography；
- LYNRED Mobility 仅 `Stereovision` 明确有外参注册信息。

## 参考资料

- [MS² Data Format](https://sites.google.com/view/multi-spectral-stereo-dataset/data-format)
- [ViViD++ Calibration](https://visibilitydataset.github.io/3_calibration.html)
- [ViViD++ Calibration Results](https://urserver.kaist.ac.kr/publicdata/ViViD++/calibration/calibration_results.zip)
- [TartanRGBT README / Calibration](https://huggingface.co/datasets/theairlabcmu/TartanRGBT/blob/main/README.md)
- [LYNRED Mobility Dataset](https://www.lynred.com/lynred-mobility-dataset)
- [BU-TIV Dataset](https://csr.bu.edu/BU-TIV/BUTIV.html)
- [BU-TIV Davis DLT Calibration](https://csr.bu.edu/BU-TIV/data/davis/July2008/calibration_dlt.xml)
- [FLIR ADAS Dataset](https://oem.flir.com/en-gb/solutions/automotive/adas-dataset-form/)
- [LTIR v1.0](https://isy.gitlab-pages.liu.se/cvl/en/datasets/ltir/version1.0/)
- [ZUT-FIR-ADAS](https://github.com/pauliustumas/ZUT-FIR-ADAS)
